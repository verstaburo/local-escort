include ../textarea/textarea

mixin send-form(placeholder, buttonValue)
    +b.FORM(action="#" method="POST").send-form&attributes(attributes)
        +textarea('message', placeholder)
            block
        +e.BUTTON.button=buttonValue
